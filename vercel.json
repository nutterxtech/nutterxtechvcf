{
  "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @workspace/vcf-registration run build && node artifacts/api-server/build-vercel.mjs",
  "outputDirectory": "artifacts/vcf-registration/dist/public",
  "framework": null,
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" }
  ],
  "functions": {
    "api/index.mjs": {
      "memory": 512,
      "maxDuration": 15
    }
  }
}
